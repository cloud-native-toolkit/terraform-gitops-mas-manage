apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Values.subscriptions.massapp.name }}
spec:
  name: {{ .Values.subscriptions.massapp.name }}
  source: {{ .Values.subscriptions.massapp.subscription.source }}
  sourceNamespace: {{ .Values.subscriptions.massapp.subscription.sourceNamespace }}
  channel: {{ .Values.subscriptions.massapp.subscription.channel }}
  installPlanApproval: {{ .Values.subscriptions.massapp.subscription.installPlanApproval }}
