apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibm-masapp-manage.fullname" . }}-test-connection"
  labels:
    {{- include "ibm-masapp-manage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ibm-masapp-manage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
